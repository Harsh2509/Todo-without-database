This is a Todo page website. It uses .json file to store data of todo tasks and a server to accept requests related to CRUD operations (update is not defined for now). Will update the repo if needed.
